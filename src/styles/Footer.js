import { StyleSheet } from "react-native";

export const Footer = StyleSheet.create({
    iconHomeView:{
        flexDirection:'row',
        alignItems: 'center',
        justifyContent: 'center',
    }
})